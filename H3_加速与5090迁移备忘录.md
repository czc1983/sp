# H3 出片加速与 5090 迁移备忘录

> 整理日期：2026-08-06 ｜ 适用范围：MiniMax H3 R2V 出片链（ComfyUI-H3）

---

## 一、八组加速组合实测（4090，480×864，20 步，124 帧，同种子同参考）

三个加速件：**S** = Sage Attention 补丁（KJNodes），**C** = BlockCache T8，**H** = NB-H3 HyperStep 跳步补丁（Turbo 档 Skip 36 Blocks）。

| 排名 | 组合 | 总耗时 | 相对基线 | 画质 |
|---|---|---|---|---|
| 1 | G = C+H | 351 s | 2.97× | ❌ 腰部乱码（禁用） |
| 2 | B = S+C+H | 412 s | 2.53× | ❌ 腹部乱码（禁用） |
| 3 | **F = S+H** | 462 s | 2.26× | ✅ 干净（→ 超分·急速） |
| 4 | A = H 单独 | 513 s（采样墙） | ~2.0× | ✅ 干净 |
| 5 | **C = S+C** | 609 s | 1.71× | ✅ 干净（→ 超分） |
| 6 | E = C 单独 | 641 s | 1.63× | ✅ 干净 |
| 7 | H0 = 裸基线 | 1042 s | 1.0× | ✅ 干净 |
| 8 | D = S 单独 | 1129 s | 0.92× 更慢 | ✅ 干净 |

**结论：**
- **Cache × HyperStep 物理冲突，禁用**。两补丁抢同一批 DiT block（日志有 `external DiT block replacements` 警告），HyperStep 跳过数 504→288 + fallback，缓存复用错误特征 → 全片固定位置乱码。B、G 都中招。
- Sage 单独用是负优化（此 int8 模型瓶颈不在注意力 kernel），但与 HyperStep 搭配快 13% 且无害。
- 可用最优：**速度选 S+H，稳定选 S+C**。加速不是乘法叠加，是二选一。

## 二、4090 → 5090 迁移实测（同参数，含 FlashVSR 超分尾，出片 960×1728）

| 工作流 | 4090 | 5090 | 提升 |
|---|---|---|---|
| 超分（S+C+FlashVSR） | 采样 594 s | 采样 193 s，**整条 338 s** | ~3.1× |
| 超分·急速（S+H+FlashVSR） | 采样 448 s | 采样 151 s，**整条 219 s** | ~3.0× |

- 5090（32G，torch 2.13.0+cu132）模型初始化 15 s，4090 要 4 分多钟——冷启动惩罚基本消失。
- **5090 端到端参考：急速 ≈ 3.6 分钟/条，超分 ≈ 5.6 分钟/条**（5 秒 960×1728 成片）。
- 5090 必须 **sageattention ≥ 2.2**（源码编译，支持 sm120）；镜像自带的 1.0.6 会报
  `sageattention is not new enough version or could not determine CUDA architecture`。

## 三、新机迁移清单（照着走）

1. 确认模型齐全：`models/diffusion_models`、`text_encoders`、`vae`、`FlashVSR-v1.1/`（约 6.8 GB）。
2. 确认参考视频在 `input/`（如 `8月5日(2).mp4`）。
3. **Sage 修复**：若 Sage 节点报错 → `bash /root/setup_sage_5090.sh`（自动探测架构、编译、冒烟测试；幂等）。
4. 重启 ComfyUI：
   ```bash
   pkill -f "faulthandler [m]ain.py"; sleep 3
   cd /root && (setsid nohup bash /root/start_h3.sh > /root/comfyui_h3.log 2>&1 < /dev/null &)
   ```
5. 工作流库导入：`超分.json`（稳）、`超分·急速.json`（快），本地母本在 `E:\sp\chaofen.json` / `E:\sp\chaofen_jisu.json`。
6. 上传接口注意：`/userdata/` 路径里的 `/` 要 `%2F` 编码（`quote(path, safe='')`），否则 405。

## 四、关键参数存档

- 分辨率 480×864，20 步（simple / res_multistep），124 帧 @ 24 fps ≈ 5.2 s
- HyperStep：Turbo - Skip 36 Blocks（bypass 7~43，refresh 4）
- BlockCache：threshold 0.12，区间 8%~95%，max_hits 2，cache_device cpu
- FlashVSR：FlashVSR-v1.1，tiny，2× → 960×1728
- 主模型 minimax_h3_ref2va_pruned_int8_convrot ｜ TE qwen3vl_32b_minimax_h3_nvfp4_awq

## 五、已知现象（非故障）

- 首次运行卡在 `MiniMax H3 Reference to Video`：懒加载在搬 VAE/TE 约 20 GB 权重，正常。
- 同种子跨组合/跨机器内容会发散（服装变化），属扩散模型数值混沌。
- HyperStep 报告 `full=6, partial=14, fallbacks=0, bypassed_blocks=504` 为健康；出现 fallbacks≥1 或 bypassed=288 即 Cache 冲突征兆。
- 测试产物存档：`E:\sp\abtest\`（八组对照 + 5090 两组，含 compare_all.png、compare_5090.png）。

---

## 六、Turbo LoRA 4 步链路实测（2026-08-06，5090，ref2va 全量 34GB）

| 链路 | 热机耗时 | 音频 | 画质 |
|---|---|---|---|
| **turbo 4 步**（全量模型 + turbo LoRA + 双时钟） | **60.9 s** | 正常（平坦度 0.18，无削波） | 干净无乱码 |
| 老链路 20 步（pruned + BlockCache） | 121.5 s | 正常（平坦度 0.22，无削波） | 干净无乱码 |

- turbo 比现役产线链**再快一倍**（60.9s vs 121.5s），即约 **1 分钟/条**。
- 爆音担忧未复现：双时钟采样器下 4 步音频正常（shift video 12.0 / audio 3.0）。
- 同种子下两链内容发散明显（不同模型+LoRA，正常），turbo 色彩更艳、细节干净。
- 34GB 全量模型在 5090 32G 上正常 offload，未吃掉的加速收益。
- **生产策略（按计划 T4）：turbo 默认关，逐项目用 `H3_TURBO=on` 开启**；产线 server.py 无需改动。
- 产物存档：`abtest/turbo_ref2va_2026-08-06/`（t1_4step.mp4、t2_oldchain.mp4、t2_compare.png）。
